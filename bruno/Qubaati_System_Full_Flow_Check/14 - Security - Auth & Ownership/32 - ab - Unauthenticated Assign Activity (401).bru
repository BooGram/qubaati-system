meta {
  name: ab - Unauthenticated Assign Activity (401)
  type: http
  seq: 32
}

post {
  url: {{baseUrl}}/activity-assignments/assign-student
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {"activityId":1,"studentId":1,"dueDate":"2027-01-15T10:00:00"}
}

tests {
  test('expected 401', () => expect(res.getStatus()).to.eql(401));
}
