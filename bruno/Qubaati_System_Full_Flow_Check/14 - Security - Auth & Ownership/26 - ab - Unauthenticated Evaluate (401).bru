meta {
  name: ab - Unauthenticated Evaluate (401)
  type: http
  seq: 26
}

post {
  url: {{baseUrl}}/ai/activity-submissions/evaluate
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {"submissionId":999999}
}

tests {
  test('expected 401', () => expect(res.getStatus()).to.eql(401));
}
