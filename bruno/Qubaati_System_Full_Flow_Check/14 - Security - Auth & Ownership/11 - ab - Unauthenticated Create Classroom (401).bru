meta {
  name: ab - Unauthenticated Create Classroom (401)
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/classrooms/add
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {"name":"Blocked Classroom","gradeLevel":6,"section":"A"}
}

tests {
  test('no credentials creating a classroom -> 401', () => expect(res.getStatus()).to.eql(401));
}
