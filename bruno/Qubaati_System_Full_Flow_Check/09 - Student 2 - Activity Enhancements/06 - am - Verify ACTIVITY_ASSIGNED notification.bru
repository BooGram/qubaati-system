meta {
  name: am - Verify ACTIVITY_ASSIGNED notification
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/users/me/notifications
  body: none
  auth: basic
}

auth:basic {
  username: {{studentUsername}}
  password: {{studentPassword}}
}

tests {
  const json = res.getBody();
  test('status 200', () => expect(res.getStatus()).to.equal(200));
  test('student got an ACTIVITY_ASSIGNED notification', () => expect(json.some(n => n.type === 'ACTIVITY_ASSIGNED')).to.eql(true));
}
