meta {
  name: ah - Decide Step 2 (Mission 2, final)
  type: http
  seq: 22
}

post {
  url: {{baseUrl}}/mission-sessions/decisions
  body: json
  auth: basic
}

auth:basic {
  username: {{adminUsername}}
  password: {{adminPassword}}
}

headers {
  Content-Type: application/json
}

body:json {
  {"choiceId": {{choiceId2}}, "reason": "Ran a load test.", "sessionId": {{sessionId}}}
}

tests {
  test('status 200', () => expect(res.getStatus()).to.equal(200));
  test('complete-ready', () => expect(res.getBody().missionCompleteReady).to.eql(true));
}
